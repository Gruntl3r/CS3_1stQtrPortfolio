let age = 13;
let maxAge = 100;
let numPerDay = 20;

let totalRequired = (numPerDay * 365) * (maxAge - age);

document.write("You will need " + totalRequired + " to last until you ripe old age of " + maxAge);